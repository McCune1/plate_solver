# ansys_queue_manifest_rect_ff_oop_mac.txt -- ISOLATED manifest for the
# rectangular FFFF OUT-OF-PLANE (flexural) MAC/eigenvector-extraction decks
# (l/b = 1.0, 1.5, 2.0, 2.5, 3.0). Separate from ansys_queue_manifest.txt,
# ansys_queue_manifest_rect_ff_ip.txt, extra_lob, and p5phaseA.
#
# ONE ANSYS license seat: do NOT submit this at the same time as any other
# MAPDL job. Run one queue to completion, then the other. Check squeue for
# any job named ansys_* before submitting.
#
# Added 2026-09-06. Purpose: Paper 2 rigor addition -- FE eigenvector
# (MAC) cross-check against this project's analytical OOP mode shapes,
# the third of three rigor items requested alongside the Leissa/Gorman
# literature comparison and the new Related Work section. Each deck below
# runs TWO passes (SYMM then ASYM cut BC) at mesh2 (the finer of the two
# previously-converged meshes from the original frequency-only decks),
# NMODES=20 per pass, and dumps nodal (X,Y,UZ) for every mode plus a
# frequency/Lambda_FE table per pass. Generated by gen_mac_decks.py, not
# hand-retyped -- see that script's own header comment for full design
# rationale, including the *VWRITE-inside-*DO-loop bug this format
# deliberately avoids (present in the OLD ansys_rect_ff_oop_halfmodel_lob*.inp
# decks -- flagged, not fixed, since those decks are out of scope here).
#
# Output per deck (10 files): rect_ff_oop_mac_{symm,asym}_freqs_lob<tag>.txt
# plus rect_ff_oop_mac_{symm,asym}_m<1..20>_lob<tag>.txt (40 mode-shape
# files) -- 50 files/deck x 5 decks = 250 output files total, each a few
# hundred KB.
#
# After the run: off-cluster Python computes MAC between these FE UZ
# eigenvectors and this project's analytical OOP mode shapes, same
# workflow as Paper 1's SUBDOM/MAC bakeoff. That script has not been
# written yet -- it is the next step after this data lands.
ansys_rect_ff_oop_mac_extract_lob100.inp
ansys_rect_ff_oop_mac_extract_lob150.inp
ansys_rect_ff_oop_mac_extract_lob200.inp
ansys_rect_ff_oop_mac_extract_lob250.inp
ansys_rect_ff_oop_mac_extract_lob300.inp
